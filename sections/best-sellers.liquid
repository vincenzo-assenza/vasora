<section class="best-sellers"{% if section.settings.anchor_id != blank %} id="{{ section.settings.anchor_id | escape }}"{% endif %}>
  <div class="best-sellers__inner">
    <div class="best-sellers__heading">
      {% if section.settings.eyebrow != blank %}
        <p class="best-sellers__eyebrow">{{ section.settings.eyebrow }}</p>
      {% endif %}
      {% if section.settings.heading != blank %}
        <h2 class="best-sellers__title">{{ section.settings.heading }}</h2>
      {% endif %}
      <div class="best-sellers__tabs" role="tablist" aria-label="Best sellers tabs">
        <button class="best-sellers__tab is-active" type="button" data-tab="women" role="tab" aria-selected="true">
          {{ section.settings.tab_label_1 }}
        </button>
        <button class="best-sellers__tab" type="button" data-tab="men" role="tab" aria-selected="false">
          {{ section.settings.tab_label_2 }}
        </button>
      </div>
    </div>

    <div class="best-sellers__grid" data-grid>
      {% for block in section.blocks %}
        <article class="best-sellers__card" data-tab="{{ block.settings.tab }}" {{ block.shopify_attributes }}>
          {% if block.settings.badge != blank %}
            <span class="best-sellers__badge">{{ block.settings.badge }}</span>
          {% endif %}
          <div class="best-sellers__media">
            {% if block.settings.image != blank %}
              {{
                block.settings.image
                | image_url: width: 900
                | image_tag: loading: 'lazy', widths: '360, 540, 720, 900', alt: block.settings.title
              }}
            {% else %}
              {{ 'product-1' | placeholder_svg_tag: 'placeholder' }}
            {% endif %}
          </div>
          <div class="best-sellers__info">
            {% if block.settings.title != blank %}
              <h3>{{ block.settings.title }}</h3>
            {% endif %}
            {% if block.settings.price != blank %}
              <p class="best-sellers__price">{{ block.settings.price }}</p>
            {% endif %}
          </div>
        </article>
      {% endfor %}
    </div>

    {% if section.settings.cta_label != blank %}
      <div class="best-sellers__cta">
        <a href="{{ section.settings.cta_link }}" class="best-sellers__cta-button">{{ section.settings.cta_label }}</a>
      </div>
    {% endif %}
  </div>
</section>

{% stylesheet %}
  .best-sellers {
    background: #f4f4f2;
    padding: clamp(3rem, 6vw, 6rem) 0;
  }
  .best-sellers__inner {
    width: min(100% - 2 * var(--container-padding-inline), var(--container-max));
    margin: 0 auto;
    display: grid;
    gap: var(--space-6);
    text-align: center;
  }
  .best-sellers__eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.24em;
    font-size: 0.7rem;
    color: var(--color-muted);
    margin: 0 0 var(--space-2);
  }
  .best-sellers__title {
    margin: 0 0 var(--space-4);
    font-size: clamp(1.4rem, 2.4vw, 2rem);
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }
  .best-sellers__tabs {
    display: inline-flex;
    gap: var(--space-5);
    border-bottom: 1px solid color-mix(in srgb, var(--color-foreground) 12%, transparent);
    padding-bottom: var(--space-2);
    margin: 0 auto;
  }
  .best-sellers__tab {
    background: none;
    border: 0;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    font-size: 0.75rem;
    padding: 0;
    cursor: pointer;
    color: var(--color-foreground);
  }
  .best-sellers__tab.is-active {
    font-weight: 600;
    position: relative;
  }
  .best-sellers__tab.is-active::after {
    content: '';
    position: absolute;
    left: 0;
    right: 0;
    bottom: -10px;
    height: 1px;
    background: var(--color-foreground);
  }
  .best-sellers__grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: clamp(1.5rem, 2vw, 2.5rem);
  }
  .best-sellers__card {
    display: grid;
    gap: var(--space-3);
    align-items: end;
  }
  .best-sellers__badge {
    text-transform: uppercase;
    font-size: 0.65rem;
    letter-spacing: 0.24em;
    color: var(--color-muted);
  }
  .best-sellers__media {
    background: #f4f4f2;
    min-height: 200px;
    display: grid;
    place-items: center;
  }
  .best-sellers__media img,
  .best-sellers__media svg {
    width: 100%;
    height: auto;
    max-width: 220px;
  }
  .best-sellers__info h3 {
    margin: 0;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    font-size: 0.75rem;
  }
  .best-sellers__price {
    margin: var(--space-1) 0 0;
    color: var(--color-muted);
    font-size: 0.8rem;
  }
  .best-sellers__cta {
    margin-top: var(--space-4);
  }
  .best-sellers__cta-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.75rem 2.2rem;
    background: #111;
    color: #fff;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    font-size: 0.7rem;
    text-decoration: none;
  }

  @media (max-width: 1024px) {
    .best-sellers__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }
  @media (max-width: 640px) {
    .best-sellers__grid {
      grid-template-columns: 1fr;
    }
  }
{% endstylesheet %}

{% javascript %}
  const bestSellers = document.currentScript.closest('.shopify-section');
  if (bestSellers) {
    const tabs = bestSellers.querySelectorAll('[data-tab]');
    const tabButtons = bestSellers.querySelectorAll('.best-sellers__tab');
    const applyTab = (tab) => {
      tabButtons.forEach((btn) => {
        const active = btn.dataset.tab === tab;
        btn.classList.toggle('is-active', active);
        btn.setAttribute('aria-selected', active ? 'true' : 'false');
      });
      tabs.forEach((card) => {
        const show = card.dataset.tab === tab;
        card.style.display = show ? '' : 'none';
      });
    };
    tabButtons.forEach((btn) => {
      btn.addEventListener('click', () => applyTab(btn.dataset.tab));
    });
    applyTab('women');
  }
{% endjavascript %}

{% schema %}
{
  "name": "Best sellers",
  "settings": [
    {
      "type": "text",
      "id": "anchor_id",
      "label": "Anchor ID",
      "default": "best-sellers"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Our best sellers"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Women    Men"
    },
    {
      "type": "text",
      "id": "tab_label_1",
      "label": "Tab label 1",
      "default": "Women"
    },
    {
      "type": "text",
      "id": "tab_label_2",
      "label": "Tab label 2",
      "default": "Men"
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "CTA label",
      "default": "All women's bags"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "CTA link"
    }
  ],
  "blocks": [
    {
      "type": "product_card",
      "name": "Product card",
      "settings": [
        {
          "type": "select",
          "id": "tab",
          "label": "Tab",
          "options": [
            { "value": "women", "label": "Women" },
            { "value": "men", "label": "Men" }
          ],
          "default": "women"
        },
        {
          "type": "text",
          "id": "badge",
          "label": "Badge",
          "default": "New"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Le mini Dalia"
        },
        {
          "type": "text",
          "id": "price",
          "label": "Price",
          "default": "€301,95"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Best sellers",
      "category": "Aurum",
      "blocks": [
        { "type": "product_card", "settings": { "tab": "women", "badge": "New", "title": "Le mini Dalia Black Liégé", "price": "€301,95" } },
        { "type": "product_card", "settings": { "tab": "women", "badge": "New", "title": "Le Gina Petrol Liégé", "price": "€327,95" } },
        { "type": "product_card", "settings": { "tab": "men", "badge": "New", "title": "Le sac elegant brown", "price": "€327,95" } },
        { "type": "product_card", "settings": { "tab": "men", "badge": "Best seller", "title": "Le Grand Nova Ivory", "price": "€387,95" } }
      ]
    }
  ]
}
{% endschema %}
